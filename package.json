{
  "name": "anonymous-lettering-generator",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "concurrently --kill-others \"npm run start:dev --workspace=api\" \"npm run start:dev --workspace=client\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "client",
    "api"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
